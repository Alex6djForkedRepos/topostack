{
  "name": "@topostack/data-contracts",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Data-layer contracts shared by the browser, the map-api Worker, and the provisioning scripts: catalog validation, archive release parsing, terrain PNG decoding, usage events, feedback submissions, traced depth charts, share-link encoding, and the changelog. Source-only; every consumer compiles TypeScript itself.",
  "exports": {
    "./archive-release": "./src/archive-release.ts",
    "./changelog": "./src/changelog.ts",
    "./chart-bathymetry": "./src/chart-bathymetry.ts",
    "./feedback": "./src/feedback.ts",
    "./share-link": "./src/share-link.ts",
    "./source-catalog": "./src/source-catalog.ts",
    "./terrain-png": "./src/terrain-png.ts",
    "./usage": "./src/usage.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "sideEffects": false
}
