# Third-party licences: TopoStack nest engine

The WebAssembly nest engine (`pkg/topostack_nest_wasm_bg.wasm`) is compiled from the crates below.
It is generated by `cargo about generate about.hbs -o THIRD_PARTY_LICENSES.md`, so do not edit it by hand.

sparrow (MIT, © 2025 Jeroen Gardeyn, KU Leuven) and jagua-rs (MPL-2.0, Jeroen Gardeyn) are used unmodified.
sparrow source code: https://github.com/JeroenGar/sparrow (revision pinned in `Cargo.toml`).
jagua-rs source code for the exact version compiled in: https://crates.io/crates/jagua-rs/0.8.3 and https://github.com/JeroenGar/jagua-rs.

## Licences used

{{#each overview}}
- {{name}}: {{count}} crate{{#if (ne count 1)}}s{{/if}}
{{/each}}

{{#each licenses}}
## {{name}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{crate.repository}}){{/if}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
