---
type: fix
title: Use shared theme controls in contour editing
---
Contour repair now uses the shared buttons, fields, selects, and checkbox styles. The review-draft picker uses a themed action instead of a native file input that widened the sidebar, eliminating horizontal scrolling at desktop and narrow widths.
